#!/usr/bin/env node

import chalk from 'chalk';
import app from '../app';

/**
 * Port
 */

const port = process.env.PORT || 9000;

/**
 * Run output
 */

const msg = [
  '\n',
  '🎉',
  ' ',
  port,
  '\n',
].join(' ');

/**
 * Listen...
 */

app.listen(port, () => {
  console.log(msg);
});

