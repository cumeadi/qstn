#!/usr/bin/env node

import app from '../app';

/**
 * Port
 */

const port = process.env.PORT || 4000;

/**
 * Run output
 */

const msg = [
  '\n',
  '🎉',
  ' ',
  port,
  '\n',
].join(' ');

/**
 * Listen...
 */

app.listen(port, () => {
  console.log(msg);
});

