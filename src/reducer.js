
/**
 * Reducer
 *
 * @param {Object} state
 * @param {Object} action
 * @return {Object}
 * @api public
 */

export default (state, {type,value}) => {
  const next = {...state};

  switch(type) {}

  return next;
};

